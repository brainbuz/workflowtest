use 5.008;

use ExtUtils::MakeMaker;

#use Devel::AssertOS qw( -MSWin32 -Cigwin);

BEGIN {
	eval 'use ExtUtils::MakeMaker::Coverage';
}

WriteMakefile(
    NAME            => 'workflowtest',
    VERSION_FROM    => 'lib/workflowtest.pm'
);